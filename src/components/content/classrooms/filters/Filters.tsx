import React, { useState } from "react";import { Collapse, Radio } from "antd";import styles from "./filters.module.css";import { useDispatch, useSelector } from "react-redux";import { setClassroomsFilter } from "../../../../store/actions";const { Panel } = Collapse;const Filters = () => {  let dispatch = useDispatch();  // @ts-ignore  let filter = useSelector((state) => state.classroomsReducer.classroomsFilter);  let radioStyle = {    display: "block",    height: "30px",    lineHeight: "30px",  };  let onChange = (e: any) => {    dispatch(setClassroomsFilter(e.target.value));  };  return (    <div className={styles.filters}>      <Collapse ghost>        <Panel header="Фільтри" key="1">          <Radio.Group onChange={onChange} value={filter}>            <Radio style={radioStyle} value={"ALL"}>              Всі аудиторії            </Radio>            <Radio style={radioStyle} value={"FREE"}>              Вільні            </Radio>            <Radio style={radioStyle} value={"SPECIAL"}>              Спеціалізовані            </Radio>            <Radio style={radioStyle} value={"CHAIR"}>              Кафедральні            </Radio>          </Radio.Group>        </Panel>      </Collapse>    </div>  );};export default Filters;